{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "@aws-lambda-powertools/logger": "^0.5.0",
        "@aws-lambda-powertools/metrics": "^0.5.0",
        "@aws-lambda-powertools/tracer": "^0.5.0",
        "aws-sdk": "^2.799.0"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest"
    }
}
