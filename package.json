{
    "name": "alpt-sam-demo",
    "description": "AWS Lambda PowerTools ",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "@aws-lambda-powertools/logger": "^0.5.1",
        "@aws-lambda-powertools/metrics": "^0.5.1",
        "@aws-lambda-powertools/tracer": "^0.5.1",
        "@aws-sdk/client-dynamodb": "^3.48.0",
        "@aws-sdk/lib-dynamodb": "^3.48.0",
        "aws-xray-sdk": "^3.3.4"
    },
    "devDependencies": {
        "aws-sdk": "^2.799.0",
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest"
    }
}
